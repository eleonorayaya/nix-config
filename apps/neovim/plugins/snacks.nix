{
  plugins = {
    snacks = {
      enable = true;

      # TODO why aren't these working?
      settings = {
        indent = {
          enabled = true;
          char = "|";
        };
        notifier = {
          enabled = true;
        };
      };
    };
  };
}
